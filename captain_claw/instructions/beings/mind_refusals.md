LAST TICK these links were REFUSED — fix or drop them, do NOT repeat them:
{refusal_lines}
A link needs BOTH files to ALREADY exist. If you meant a file you haven't written, WRITE it for real this tick first, then link it.
