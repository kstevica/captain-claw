# Review summary — r1

Wood gathering is completely broken because TREE tiles are impassable; peasants can never reach them. Additionally, resource deposit accepts any dropoff regardless of distance, so resources can be lost without reaching the destination. The idle loop also wastes performance scanning every frame. These are correctness and performance issues that must be fixed before release.